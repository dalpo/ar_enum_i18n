require "spec_helper"

RSpec.describe ArEnumI18n do
  it "has a version number" do
    expect(Ar::Enum::I18n::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
